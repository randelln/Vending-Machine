using System;
using System.Collections.Generic;
using System.Linq;
using System.Threading.Tasks;
using VendingMachine.Models;
using VendingMachine.Services;

namespace VendingMachine.Services
{
    public class UserService
    {
        static UserService()
        {
            //Get product user selected by ID

            //ProductService product = new ProductService();

            //product.GetById()

            //Get user balance
            //Compare balance to product price, if balance less than price, send error message
            //If balance greater than or equal to product price, call update method from product service and subtract prod price from user balance
        }
    }
}
